# 📥📤 C Programming: Input and Output

## 📌 Overview
C uses functions like `scanf()` and `printf()` for input and output.

---

## 🔹 Input: `scanf()`

```c
int num;
scanf("%d", &num);
```

**Format Specifiers:**
- `%d` for integers
- `%lf` for `double`
- `%c` for characters
- `%s` for strings

**Note:** Use `" %c"` (with a space) to skip whitespace for character input.

---

## 🔸 Output: `printf()`

```c
printf("Value: %d\n", num);
```

**Format Specifiers:**
- `%d`, `%f`, `%c`, `%s`, `%0.2lf`, etc.

## ✅ Tips
- Always match format specifiers with variable types
- Newlines are added with `\n`
